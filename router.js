import express from 'express';

export const router = express.Router();

// define routes
router.get('/api/dishes', );
router.get('/api/dishes/:name', );
router.post('/api/dishes', );
router.put('/api/dishes/:id', );
router.delete('/api/dishes/:id', );